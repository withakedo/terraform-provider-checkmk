---
page_title: "checkmk_comment Resource - checkmk"
subcategory: "Operations"
description: |-
  Adds a comment to a host, or to a single service on a host.

Comments are monitoring operations, not configuration - they take effect immediately and do not
require `checkmk_activation`. Every attribute forces replacement on change: there is no
in-place "modify comment" support, so changing the comment text removes the existing comment and
creates a new one.

---

# checkmk_comment (Resource)

Adds a comment to a host, or to a single service on a host.

Comments are monitoring operations, not configuration - they take effect immediately and do not
require `checkmk_activation`. Every attribute forces replacement on change: there is no
in-place "modify comment" support, so changing the comment text removes the existing comment and
creates a new one.


## Example Usage

```terraform
terraform {
  required_providers {
    checkmk = {
      source = "withake-it/checkmk"
    }
  }
}

provider "checkmk" {
  url      = "http://localhost:5000/test"
  username = "automation"
  password = "your-secret-here"
}

resource "checkmk_host" "web" {
  host_name = "web-01.example.com"
  folder    = "/"

  attributes = {
    ipaddress = "10.1.2.3"
  }
}

# Comment on the whole host
resource "checkmk_comment" "note" {
  host_name = checkmk_host.web.host_name
  comment   = "Migrated to new datacenter rack B12 on 2026-08-24"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `comment` (String) The comment text.
- `host_name` (String) Name of the host to add the comment to.

### Optional

- `persistent` (Boolean) If true, the comment persists a CheckMK restart (default: false).
- `service_description` (String) Name of the service on `host_name` to add the comment to. If omitted, the comment applies to the host itself.

### Read-Only

- `id` (String) Unique identifier for this comment resource instance.


